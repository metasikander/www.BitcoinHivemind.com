baseurl: "" # The subpath of your site. Use "" if you're hosting a blog at the root level.
blogurl: "/blog" # Blog subpath
url: "http://www.bitcoinhivemind.com" # the base hostname & protocol for your site
title: "The Bitcoin Hivemind"

# This is used by feed.xml. Uncomment if you want to use it.
# description: "..."

gems:
  - jemoji
exclude:
  - README.md
  - CHANGELOG.md
  - CNAME
  - Gemfile
  - Gemfile.lock
permalink: /blog/:title/
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

encoding: utf-8

sass:
  sass_dir: _sass
